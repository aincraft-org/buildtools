/**
 * Platform-neutral public contracts for BuildTools.
 *
 * <p>This module must not depend on Paper, Bukkit, or NMS types. Implementations live in
 * {@code common} (JVM domain) and {@code paper} (server adapters).
 */
package dev.mintychochip.buildtools.api;
